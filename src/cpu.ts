class CPU {
    // レジスタ定義

    // ROMを読み込む
    // 511バイトまでフォントセットを埋める
    // 512バイトからはROMを読み込ませる

    // 命令コード読み込み
    // リトルエンディアン→ビッグエンディアンに変換するため最初のバイトと二つ目のバイトを入れ替える
    // c, k, y, d に分割する
    // nnn と kk を抽出する
    // 命令実行

    // タイマー実装
}